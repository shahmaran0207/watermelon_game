{
  "name": "game_project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "homepage": "https://shahmaran0207.github.io/game/",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "next build && touch out/.nojekyll && git add -f out/ && git commit -m \"Deploy\" && git subtree push --prefix out origin gh-pages"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  },
  "dependencies": {
    "gh-pages": "^6.1.1",
    "matter-js": "^0.19.0"
  }
}
